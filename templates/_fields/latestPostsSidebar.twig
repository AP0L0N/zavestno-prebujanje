<aside class="widget">
    <h2>Zadnji prispevki</h2>
    <ul>
        {% for blog in craft.entries().section('prispevki').orderBy('postDate desc').limit(5).all() %}
            {% include '_fields/_blogInfoSidebar' %}
        {% endfor %}
    </ul>
</aside>
